import { StyleSheet } from 'react-native'

const globalStyles = StyleSheet.create({
    button: {
        paddingVertical: 16,
        paddingHorizontal: 8,
        textAlign: 'center',
        borderRadius: 10
    },
    buttonText: {
        textAlign: 'center'
    }
})

export default globalStyles
